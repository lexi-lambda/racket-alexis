#lang info

(define collection 'multi)

(define name "struct-updaters")
(define version "0.1.0")

(define deps
  '("base"
    ))
(define build-deps
  '("rackunit-lib"
    "scribble-lib"
    "racket-doc"
    ))
